get_prop(shell, custom_version_prop)

# Allow shell to disable PIHooks
set_prop(shell, pih_features_prop)

# Allow shell to set sussybox prop
set_prop(shell, sussybox_fail_prop)

# Allow shell to set sussybox OS version
set_prop(shell, sussybox_osversion_prop)

